<h1>My Map</h1>


<%= simple_form_for([@map, @new_pot_loc]) do |f| %>
/maps/:map_id/potential_locations
  <div class="form-inputs">
    <%= f.input :address %>
    <%= f.input :name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

<%  @new_pot_loc.each do |loc| %>
  <%= loc.address %>
<% end %>


<%# radio buttons %>
<%# Map goes here %>
<%# link to POI page %>
<%# link to compare page %>
